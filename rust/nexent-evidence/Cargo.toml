[package]
name = "nexent-evidence"
version = "0.1.0"
edition = "2021"
description = "Deterministic evidence, invariant verification, and fail-closed containment primitives for NEXENT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
