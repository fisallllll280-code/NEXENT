[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nexent"
version = "0.1.0"
description = "NEXENT governed system fabric"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
nexent = "nexent.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
